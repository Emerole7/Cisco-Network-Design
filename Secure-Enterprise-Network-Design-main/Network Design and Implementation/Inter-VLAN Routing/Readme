By default, devices residing in different VLANs are isolated from one another and cannot communicate without a dedicated routing mechanism in place. Switch Virtual Interfaces (SVI) were configured on both core switches to handle inter-VLAN routing, each VLAN interface was assigned an IP address and encapsulation VLAN ID accordingly. The screenshot shows a sample of the configuration output.
